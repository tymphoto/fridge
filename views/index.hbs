{{#if userId}}
<div class="container">
  <div class="products d-flex justify-content-between" id="products">
  {{#each products}}
      <div class="product d-flex flex-column-reverse">
        <div class="p-2 d-flex justify-content-center">
          <div class="products mb-0 mt-0 bg-light" id="div-{{id}}">
            <img src={{this.image}} alt="product" class="productImg">
            <h6 class="text-success">{{this.name}}</h6>
            <p class="text-muted comment-text">{{this.Category.name}}</p>
            <button class="btn btn-secondary btn-sm delete-button" type="click" data-delete={{id}}>Съесть</button></li>
            <button class="btn btn-secondary btn-sm delete-button" type="click" data-update={{id}}>Изменить</button></li>
          </div>
        </div>
      </div>
  {{/each}}
</div>
</div>
{{/if}}

{{#unless userId}}

  <div class="container">
    <div class="header mt-5">
        <h4>Авторизируйтесь или зарегестрируйтесь,</h4>
        <h4>чтобы увидеть список своих продуктов</h4> 
    </div>
  </div>
  
{{/unless}}

